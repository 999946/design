declare namespace ThirdComponents {
    export interface ComponentConfig {
        /**
         * 组件名
         */
        name: string
        /**
         * 定义文件路径
         */
        typingPath: string
    }
}